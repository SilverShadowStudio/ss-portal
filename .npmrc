; react-day-picker@8 declares a peer of date-fns@2||3, but the app runs
; date-fns@4. The two work together fine at runtime; this lets strict `npm ci`
; (in CI) install past the peer conflict, matching how Vercel already builds.
legacy-peer-deps=true
